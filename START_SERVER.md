# كيفية تشغيل الخادم (Backend Server)

## المشكلة
إذا رأيت رسالة خطأ: `Backend server is not running` أو `ERR_CONNECTION_REFUSED`، فهذا يعني أن الخادم غير قيد التشغيل.

## الحل السريع

### الطريقة 1: استخدام ملف Batch (الأسهل)
1. افتح مجلد `project/server`
2. انقر نقراً مزدوجاً على `start-server.bat`
3. انتظر حتى ترى رسالة: `🚀 API Server running on http://localhost:3001`

### الطريقة 2: استخدام Terminal
1. افتح Terminal (PowerShell أو Command Prompt)
2. انتقل إلى مجلد الخادم:
   ```powershell
   cd "E:\New folder\sauidi tower\project\server"
   ```
3. شغّل الخادم:
   ```powershell
   npm run dev
   ```
4. انتظر حتى ترى رسالة: `🚀 API Server running on http://localhost:3001`

## التحقق من أن الخادم يعمل

افتح المتصفح وانتقل إلى:
```
http://localhost:3001/health
```

يجب أن ترى:
```json
{
  "status": "ok",
  "database": "connected"
}
```

## المتطلبات

1. **Node.js** (الإصدار 18 أو أحدث)
2. **PostgreSQL** يجب أن يكون قيد التشغيل
3. ملف `.env` موجود في مجلد `server` ويحتوي على:
   ```
   DATABASE_URL=postgresql://postgres:1111@localhost:5432/saudi_towerdb
   PORT=3001
   JWT_SECRET=your-secret-key-change-in-production
   ```

## استكشاف الأخطاء

### الخادم لا يبدأ
- تحقق من أن PostgreSQL يعمل
- تحقق من أن ملف `.env` موجود وصحيح
- تحقق من أن المنفذ 3001 غير مستخدم من قبل برنامج آخر

### خطأ في الاتصال بقاعدة البيانات
- تأكد من أن PostgreSQL يعمل
- تحقق من `DATABASE_URL` في ملف `.env`
- تأكد من أن قاعدة البيانات `saudi_towerdb` موجودة

### المنفذ 3001 مستخدم
- أغلق أي برنامج آخر يستخدم المنفذ 3001
- أو غيّر `PORT` في ملف `.env` إلى منفذ آخر (مثل 3002)

## ملاحظات

- **لا تغلق Terminal** الذي يعمل فيه الخادم
- الخادم يجب أن يبقى قيد التشغيل أثناء استخدام التطبيق
- لإيقاف الخادم، اضغط `Ctrl+C` في Terminal

